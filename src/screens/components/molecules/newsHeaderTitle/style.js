import {StyleSheet} from 'react-native';
import color from '_theme/colors';

export default StyleSheet.create({
  container: {
    paddingVertical: 12,
  },
  text: {
    fontSize: 32,
    fontWeight: '900',
  },
});
