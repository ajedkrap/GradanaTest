import React from 'react';
import CameraOptions from '_templates/cameraOptions';

const CameraOptionsScreen = props => {
  return <CameraOptions {...props} />;
};

export default CameraOptionsScreen;
